# 위치: docker/prometheus/prometheus.yml

global:
  scrape_interval: 15s        # 15초마다 한 번씩 긁는다

scrape_configs:
  - job_name: 'legend-momo-city'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['host.docker.internal:8080']